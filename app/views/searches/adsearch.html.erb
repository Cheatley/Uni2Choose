<!--Functionality of recommender?? --> 
<% provide(:title, '') %>

<!--containers for design/layout --> 
<div class = "contenty">
  <div class = "row">
    <h2>Search</h2>
    </div>
     <div class = "row">
    <div class="col-s-4 col-s-4";>

    <!--Page information --> 

<%= search_form_for @adsearch, url: adsearch_path, method: :get do |f| %>

          <%= label_tag :uregion, "Refine by region:" %><br>
          <%= select_tag :uregion, options_for_select(["Northern", "Central belt", "South from Dundee"])  %>

  <%= f.condition_fields do |c| %>
    <%= render "condition_fields", f: c %>
  <% end %>
  <p><%= link_to_add_fields "Add Conditions", f, :condition %></p>
  <div class="field">
    Sort:
    <%= f.sort_fields do |s| %>
      <%= s.sort_select %>
    <% end %>
  </div>
  <div class="actions"><%= f.submit "Search" %></div>
<% end %>
</div>

    <div class="col-s-8 col-s-8";>


      <% if @data != nil %>
      <p><%= @data.count %> results</p>
      <% end %>


      <% if @data != nil %>
        <table border="1" class="table">
          <thead>
            <tr>
              <th>University Name</th>
              <th>Course Name</th>
              <th>Duration</th>
              <th>Qualification</th>
              <th>Entry Requirements</th>
            </tr>
          </thead>
          <tbody>
            <% @data.each do |degree| %>
            <tr>
              <td><%= degree.uname %></td>
              <td><%= degree.cname %></td>
              <td><%= degree.duration %></td>
              <td><%= degree.qualification %></td>
              <td><%= degree.entry %></td>
            </tr>
            <% end %>
          </tbody>
        </table>
      <% end %>
      
      </div>
      </div>
